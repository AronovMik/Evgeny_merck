---
name: Langdock — промпт ассистента
description: Точная копия системного промпта из ассистента Langdock. Вставить текст ниже, под шапкой. Файлы не подключаются — прогон побайтово эквивалентен прямому вызову API.
order: 1
model: gpt-5.5
---

ВСТАВЬТЕ СЮДА ТЕКСТ ИНСТРУКЦИЙ ИЗ АССИСТЕНТА LANGDOCK — целиком, как есть,
без правок и сокращений.

Всё, что находится ниже шапки `---`, уходит в API как system-сообщение
без единого добавленного символа. Ни песочница, ни этот комментарий в запрос
не попадают — но сам комментарий удалите, иначе он станет частью промпта.
